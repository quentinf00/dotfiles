#!/bin/bash
eval "$(~/.local/bin/mise activate bash)"
export PATH="$HOME/.pixi/bin:$PATH"

alias t='$(which zsh) tmux new -As home'

if [  "`which tmux`" ] && [ "`which zsh`" ]  && [ "$TMUX" == "" ]; then
	t
fi
